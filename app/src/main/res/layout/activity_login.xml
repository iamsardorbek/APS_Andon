<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Login">

    <EditText
        android:id="@+id/access_code"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginVertical="30dp"
        android:ems="10"
        android:gravity="center_horizontal"
        android:hint="Ваш код доступа"
        android:inputType="textPersonName"
        android:textAlignment="center" />

    <Button
        android:id="@+id/check_this_point"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Проверить данный пункт"
        android:layout_below="@+id/access_code"/>

    <Spinner
        android:id="@+id/spinner_pults"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/access_code"
        android:prompt="@string/spinner_pults_prompt" />

    <TextView
        android:id="@+id/loading"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:text="Подождите пока загрузятся\nданные с базы данных"
        android:layout_above="@+id/spinner_pults"
        android:gravity="center_horizontal"
        />
</RelativeLayout>